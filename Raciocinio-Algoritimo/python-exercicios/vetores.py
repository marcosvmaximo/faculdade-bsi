# Vetores em python comporta 1 ou + tipos em um mesmo vetor, sendo considerado então um vetor não tipado, ou seja, ele não será de um tipo em especifico.. 
# Mas você pode monta-lo com apenas um tipo também.


# Métodos de vetores

#sum(vetor) = Soma de todos itens do vetor

#len(vetor) Média de todos elementos do vetor.

# Para iterar um vetor = for elemento in vetor:

# enumerate() = Itera sobre um valor usando o valor e o indice como parametros :
# for index, valor in enumerate(vetor):

# Para iterar um vetor por um range de tamanho = for i in range(comeco, fim)

# max(vetor) = retorna o valor maximo de um vetor.

# vetor.index(valor) = retorna o indice (local) do valor passado


#######################################################################################
# Exercício

# 1 - Captcha:
# • Implementar um programa em Python capaz de gerar um Captcha e permitir sua avaliação.

# Detalhes
# • O programa deverá ser capaz de gerar um captcha de 6 caracteres. O captcha
# obrigatoriamente deve conter letras (a-z) e números (0-9).
# • Após sua geração, o programa deve permitir a leitura de um conjunto de caracteres pelo
# teclado e a comparação com o captcha anteriormente gerado.

# 2 - 
# Implemente um programa em Python que seja capaz de criar uma
# string e imprimir na tela:
# • quantas vezes foram encontradas as letras vogais;
# • quantos espaços existem no texto.
