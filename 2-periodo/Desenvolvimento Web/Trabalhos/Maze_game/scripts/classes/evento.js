// Classe Evento
export class Evento {
  // O construtor recebe uma descrição do evento como argumento.
  constructor(descricao) {
    this.descricao = descricao; // Inicializa a propriedade "descricao" da instância da classe Evento.
  }
}
